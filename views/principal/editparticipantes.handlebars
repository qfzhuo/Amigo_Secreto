{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h3>Editar categoria</h3>
<div class="card">
    <div class="card-body">
<form action="/participantes/edit" method="POST">
    <input type="hidden" name="id" value="{{participante._id}}">
    <label for="nome">Nome: </label>
        <input type="text" id="nome" name="nome" placeholder="Nome da categorias" class="form-control" value="{{partcipante.nome}}">
    <label for="email">Email: </label>
        <input type="text" id="email" name="email" placeholder="Email do participante" class="form-control" value="{{participantes.slug}}">
        <br>
    <button type="submit" class="btn btn-success">Editar participante</button>
</form>
</div>
</div>